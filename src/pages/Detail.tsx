import { DetailView } from '../components/features/DetailView';

export const Detail = () => {
    return (
        <div className="animate-in fade-in duration-500">
            <DetailView />
        </div>
    );
};
